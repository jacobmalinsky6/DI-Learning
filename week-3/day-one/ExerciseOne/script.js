// Create these variables and give them values:

let adressNumber = 10;
let addressStreet = "Borochov";
let fullAdress = addressStreet + " " + adressNumber;
let city = "Givatayim";
let country = "Israel";
let reigon = city + ", " + country;

// Write a variable named globalAddress, and concatenate inside, the variables:

let globalAdress = "User location is " + fullAdress + " in " + reigon;
console.log(globalAdress);
